import { Box, Button, Text } from "@chakra-ui/react";
import { FC } from 'react';

export const TimeItem: FC = () => {
  return (
    <Box>
      <Text>人工知能</Text>
      <Button>削除</Button>
    </Box>
  );
};
